# Проверка работы AI

## Шаги для диагностики:

1. **Убедитесь, что установлены зависимости:**
```bash
cd server
npm install
```

Должны установиться:
- @google/generative-ai
- dotenv

2. **Проверьте, что backend перезапущен:**
```bash
npm run dev
```

3. **Проверьте консоль backend при отправке сообщения:**
Должны появиться логи:
- `🤖 Используем Gemini AI для ответа пациента`
- `🔑 API Key: AIzaSyAAx...`
- `📡 Отправляем запрос в Gemini API...`
- `✅ AI ответ получен: ...`

Если видите `❌ AI ответ не получен` - проверьте ошибку.

4. **Если AI не работает, проверьте:**
- Установлен ли пакет: `npm list @google/generative-ai`
- Правильный ли API ключ
- Есть ли интернет соединение
- Работает ли Gemini API (может быть временно недоступен)

5. **Временное отключение AI (для тестирования):**
Создайте файл `.env` в папке `server/`:
```
USE_AI=false
```

Это вернет rule-based логику.






# 💳 ПЛАН АВТОМАТИЗАЦИИ ОПЛАТЫ И ПОДПИСКИ

## 📋 Текущее состояние

### ✅ Что уже есть:
1. **Окно регистрации** - центрировано через CSS (`.auth-container`)
2. **Базовая структура подписки**:
   - `src/services/subscriptionService.js` - клиентский сервис
   - `server/src/routes/subscription.js` - API роуты
   - `server/src/services/subscriptionService.js` - серверная логика
3. **Система медкоинов**:
   - `src/services/medcoinsService.js` - клиентский сервис
   - `server/src/routes/medcoins.js` - API роуты
4. **Страница тарифов** - `src/components/PricingPage.js`

### ❌ Что нужно сделать:
1. Интеграция с платежным шлюзом
2. Автоматическая активация подписки после оплаты
3. Тестовый режим для разработки
4. Webhook для обработки платежей

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Этап 1: Подготовка и тестирование (СЕЙЧАС)

#### 1.1. Улучшение центрирования регистрации
- ✅ Окно уже центрировано, но можно улучшить визуально
- Добавить анимацию появления
- Улучшить адаптивность на мобильных

#### 1.2. Тестовый режим оплаты
**Цель:** Позволить тестировать оформление подписки без реальных платежей

**Что нужно:**
1. Добавить флаг `TEST_MODE=true` в `.env`
2. Создать тестовый endpoint `/api/subscription/test-activate`
3. Модифицировать `PricingPage` для показа тестового режима
4. Автоматически активировать подписку при нажатии "Оформить" в тестовом режиме

**Файлы для изменения:**
- `server/src/routes/subscription.js` - добавить тестовый роут
- `src/components/PricingPage.js` - добавить тестовый режим
- `server/.env` - добавить `TEST_MODE=true`

---

### Этап 2: Выбор платежного шлюза

#### Варианты для Казахстана:

**1. Kaspi Pay / Kaspi.kz Merchant API** ⭐ Рекомендуется
- **Плюсы:**
  - Самый популярный в Казахстане
  - Низкие комиссии (1-2%)
  - Простая интеграция
  - Поддержка рекуррентных платежей
- **Минусы:**
  - Нужна регистрация как ИП/ТОО
  - Документация на казахском/русском
- **Документация:** https://developer.kaspi.kz/

**2. Stripe** 
- **Плюсы:**
  - Отличная документация
  - Поддержка рекуррентных платежей
  - Тестовый режим из коробки
- **Минусы:**
  - Может быть сложнее для казахстанских карт
  - Комиссии выше

**3. CloudPayments (Казахстан)
- **Плюсы:**
  - Специально для СНГ
  - Поддержка тенге
- **Минусы:**
  - Меньше документации

**Рекомендация:** Начать с **Kaspi Pay** для продакшена, использовать **Stripe** для тестирования.

---

### Этап 3: Интеграция платежного шлюза

#### 3.1. Установка зависимостей

**Для Kaspi Pay:**
```bash
cd server
npm install axios
# Kaspi Pay использует REST API, axios достаточно
```

**Для Stripe:**
```bash
cd server
npm install stripe
```

#### 3.2. Структура файлов

Создать:
```
server/src/
  ├── services/
  │   ├── paymentService.js      # Основной сервис оплаты
  │   ├── kaspiPayService.js     # Интеграция Kaspi Pay
  │   └── stripeService.js       # Интеграция Stripe (опционально)
  └── routes/
      └── payment.js             # Роуты для оплаты
```

#### 3.3. Процесс оплаты

**Поток:**
1. Пользователь нажимает "Оформить подписку" → `POST /api/payment/create-session`
2. Сервер создает платежную сессию → возвращает `paymentUrl`
3. Пользователь перенаправляется на страницу оплаты
4. После успешной оплаты → webhook → `POST /api/payment/webhook`
5. Webhook активирует подписку автоматически

---

### Этап 4: Автоматизация активации

#### 4.1. Webhook обработчик

**Файл:** `server/src/routes/payment.js`

```javascript
// POST /api/payment/webhook
// Обрабатывает уведомления от платежной системы
// Автоматически активирует подписку после успешной оплаты
```

#### 4.2. Обновление подписки

**Файл:** `server/src/services/subscriptionService.js`

Добавить функцию:
```javascript
async function activateSubscriptionAfterPayment(userId, planId, paymentId) {
  // 1. Проверить статус платежа
  // 2. Активировать подписку
  // 3. Начислить медкоины
  // 4. Отправить email уведомление (опционально)
}
```

---

## 🔧 ЧТО ОТ ВАС ТРЕБУЕТСЯ

### Для тестирования (СЕЙЧАС):

1. **Ничего не требуется** - я создам тестовый режим, который будет работать автоматически

### Для продакшена (позже):

#### 1. Регистрация в платежной системе

**Kaspi Pay:**
- Зарегистрироваться как ИП или ТОО
- Подать заявку на подключение Kaspi Pay
- Получить:
  - `KASPI_MERCHANT_ID`
  - `KASPI_SECRET_KEY`
  - `KASPI_API_KEY`

**Stripe:**
- Создать аккаунт на https://stripe.com
- Получить:
  - `STRIPE_SECRET_KEY`
  - `STRIPE_PUBLISHABLE_KEY`

#### 2. Настройка переменных окружения

Добавить в `server/.env`:
```env
# Режим работы
TEST_MODE=false

# Kaspi Pay
KASPI_MERCHANT_ID=your_merchant_id
KASPI_SECRET_KEY=your_secret_key
KASPI_API_KEY=your_api_key

# Или Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
```

#### 3. Настройка Webhook URL

В панели платежной системы указать:
```
https://yourdomain.com/api/payment/webhook
```

---

## 📝 ПЛАН РЕАЛИЗАЦИИ (пошагово)

### Шаг 1: Тестовый режим (СЕЙЧАС) ✅
- [x] Создать тестовый endpoint активации
- [ ] Добавить индикатор тестового режима в UI
- [ ] Автоматическая активация при клике

### Шаг 2: Интеграция Stripe (для тестирования)
- [ ] Установить Stripe SDK
- [ ] Создать платежную сессию
- [ ] Обработка webhook
- [ ] Тестирование с тестовыми картами

### Шаг 3: Интеграция Kaspi Pay (для продакшена)
- [ ] Изучить документацию Kaspi Pay
- [ ] Создать сервис интеграции
- [ ] Настроить webhook
- [ ] Тестирование с реальными картами

### Шаг 4: Автоматизация
- [ ] Автоматическая активация после оплаты
- [ ] Email уведомления
- [ ] Логирование транзакций
- [ ] Обработка ошибок

---

## 🧪 ТЕСТИРОВАНИЕ

### Тестовые карты Stripe:
- Успешная оплата: `4242 4242 4242 4242`
- Отклоненная: `4000 0000 0000 0002`

### Тестовый режим:
- При `TEST_MODE=true` все платежи автоматически успешны
- Подписка активируется без реальной оплаты

---

## 📞 ПОДДЕРЖКА

Если возникнут вопросы:
1. Проверьте логи сервера: `server/logs/`
2. Проверьте переменные окружения
3. Проверьте статус платежа в панели платежной системы

---

## ⚠️ ВАЖНО

1. **Никогда не коммитьте** реальные ключи API в Git
2. Используйте `.env` файлы (они в `.gitignore`)
3. В продакшене используйте HTTPS
4. Валидируйте все платежи через webhook
5. Храните логи всех транзакций

---

## 🚀 БЫСТРЫЙ СТАРТ (для тестирования)

После того как я создам тестовый режим:

1. Запустите сервер: `cd server && npm run dev`
2. Запустите фронтенд: `npm start`
3. Зарегистрируйте новый аккаунт
4. Перейдите в "Тарифы"
5. Нажмите "Оформить подписку" - подписка активируется автоматически!

---

**Дата создания:** 2025-01-23
**Статус:** В разработке




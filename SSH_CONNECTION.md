# 🔐 Инструкция по подключению к серверу

## Информация о сервере

- **IP адрес:** `85.198.88.102`
- **Пользователь:** `root`
- **ОС:** Ubuntu 22.04 x64
- **SSH ключ:** `server_key.pem` (в корне проекта)

## Шаги подключения

### 1. Подготовка SSH ключа

1. Откройте файл `server_key.pem` в корне проекта
2. Вставьте ваш приватный SSH ключ (весь ключ, включая строки `-----BEGIN...` и `-----END...`)
3. Сохраните файл

### 2. Подключение через Windows

#### Вариант 1: Через командную строку (cmd)

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Перейдите в папку проекта:
   ```cmd
   cd C:\Users\USER\Documents\medpractice
   ```
4. Выполните команду подключения:
   ```cmd
   ssh -i server_key.pem root@85.198.88.102
   ```

#### Вариант 2: Через PowerShell

1. Нажмите `Win + X` и выберите "Windows PowerShell" или "Terminal"
2. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\USER\Documents\medpractice
   ```
3. Выполните команду:
   ```powershell
   ssh -i server_key.pem root@85.198.88.102
   ```

### 3. Если возникли проблемы с правами доступа

В Windows может потребоваться установить правильные права на файл ключа:

```powershell
# В PowerShell (от имени администратора)
icacls server_key.pem /inheritance:r
icacls server_key.pem /grant:r "%USERNAME%:R"
```

### 4. Альтернатива: Использовать PuTTY

Если у вас есть ключ в формате PuTTY (.ppk):

1. Скачайте PuTTY: https://www.putty.org/
2. Откройте PuTTY
3. В Host Name: `root@85.198.88.102`
4. В Connection → SSH → Auth → Credentials выберите ваш .ppk файл
5. Нажмите Open

## После подключения

После успешного подключения вы увидите приглашение командной строки Ubuntu:
```
root@server:~#
```

## Следующие шаги после подключения

1. Обновить систему:
   ```bash
   apt update && apt upgrade -y
   ```

2. Установить Node.js (если еще не установлен):
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   apt install -y nodejs
   ```

3. Установить Nginx:
   ```bash
   apt install -y nginx
   ```

4. Установить PM2 для управления Node.js процессами:
   ```bash
   npm install -g pm2
   ```

## Безопасность

⚠️ **Важно:**
- Файл `server_key.pem` содержит приватный ключ - **НЕ ЗАГРУЖАЙТЕ ЕГО В GIT!**
- Файл уже добавлен в `.gitignore`
- Храните ключ в безопасном месте
- Не делитесь ключом с другими

## Проверка подключения

После подключения проверьте:
```bash
# Проверить версию Node.js
node --version

# Проверить версию npm
npm --version

# Проверить системную информацию
uname -a
```

---

**Готово!** Теперь вы можете подключиться к серверу и начать деплой приложения.


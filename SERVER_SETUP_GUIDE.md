# 🚀 Руководство по настройке сервера

## ✅ Подключение успешно!

Вы успешно подключились к серверу Ubuntu 22.04.

## 📋 Пошаговая настройка

### Вариант 1: Автоматическая настройка (рекомендуется)

1. **Скопируйте скрипт на сервер:**

   На вашем компьютере (в PowerShell):
   ```powershell
   # Скопировать скрипт на сервер
   scp -i C:\Users\USER\.ssh\id_ed25519 server_setup.sh root@85.198.88.102:/root/
   ```

2. **На сервере выполните:**
   ```bash
   chmod +x server_setup.sh
   ./server_setup.sh
   ```

### Вариант 2: Ручная настройка

Выполните команды по порядку:

#### 1. Обновить систему
```bash
apt update && apt upgrade -y
```

#### 2. Установить Node.js 18.x
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt install -y nodejs
```

Проверьте:
```bash
node --version
npm --version
```

#### 3. Установить Nginx
```bash
apt install -y nginx
```

#### 4. Установить PM2
```bash
npm install -g pm2
```

#### 5. Установить Git
```bash
apt install -y git
```

#### 6. Настроить firewall (опционально)
```bash
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw --force enable
```

## ✅ Проверка установки

После установки проверьте:

```bash
# Node.js
node --version  # Должно показать v18.x.x или выше

# npm
npm --version   # Должно показать версию npm

# PM2
pm2 --version   # Должно показать версию PM2

# Nginx
nginx -v        # Должно показать версию Nginx
```

## 📝 Следующие шаги после настройки

1. **Клонировать репозиторий:**
   ```bash
   cd /var/www
   git clone <ваш-репозиторий-url> medpractice
   cd medpractice
   ```

2. **Настроить .env файлы:**
   ```bash
   # Backend
   cd server
   nano .env
   # Добавьте ваши переменные окружения
   ```

3. **Установить зависимости:**
   ```bash
   # В корне проекта
   npm install
   cd server && npm install && cd ..
   ```

4. **Собрать frontend:**
   ```bash
   npm run build
   ```

5. **Запустить backend:**
   ```bash
   cd server
   pm2 start src/index.js --name medpractice-api
   pm2 save
   pm2 startup
   ```

## 🔧 Настройка Nginx

После установки нужно настроить Nginx для работы с вашим приложением. См. `DEPLOYMENT_CHECKLIST.md` для детальной конфигурации.

---

**Готово!** Теперь сервер готов к деплою приложения.


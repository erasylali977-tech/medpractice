# Решение проблем

## Ошибка 404 при обращении к API

### 1. Проверьте, что backend запущен

Откройте терминал и проверьте, что видите сообщение:
```
🚀 Server running on http://localhost:3001
📡 API endpoints available at http://localhost:3001/api
```

Если не видите - backend не запустился.

### 2. Проверьте установку зависимостей backend

```bash
cd server
npm install
```

Убедитесь, что установились:
- express
- cors
- nodemon (dev dependency)

### 3. Проверьте, что порт 3001 свободен

Если видите ошибку `EADDRINUSE` - порт занят. Либо:
- Закройте другой процесс на порту 3001
- Или измените порт в `server/src/index.js`: `const PORT = process.env.PORT || 3002;`

### 4. Проверьте работу API вручную

Откройте в браузере:
- `http://localhost:3001/api/health` - должен вернуть JSON
- `http://localhost:3001/api/cases` - должен вернуть массив кейсов

Если не открывается - backend не работает.

### 5. Проверьте консоль backend на ошибки

В терминале, где запущен backend, должны быть логи запросов:
```
2024-01-01T12:00:00.000Z - GET /api/cases
```

Если видите ошибки импорта (например, `Cannot find module`) - проверьте, что все файлы на месте.

### 6. Проверьте версию Node.js

Backend использует ES modules. Нужен Node.js версии 14+:
```bash
node --version
```

### 7. Очистите кэш и переустановите

```bash
cd server
rm -rf node_modules package-lock.json
npm install
npm run dev
```

## Ошибка CORS

Если видите ошибки CORS в консоли браузера - проверьте, что в `server/src/index.js` есть:
```javascript
app.use(cors());
```

## Frontend не подключается к backend

1. Проверьте, что в `src/App.js` и `src/services/patientAI.js` правильный URL:
```javascript
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:3001';
```

2. Если backend на другом порту, создайте файл `.env` в корне проекта:
```
REACT_APP_API_URL=http://localhost:3001
```

## Chrome DevTools CSP предупреждение

Предупреждение о Content Security Policy от Chrome DevTools не критично - это просто предупреждение браузера, не ошибка приложения. Его можно игнорировать.






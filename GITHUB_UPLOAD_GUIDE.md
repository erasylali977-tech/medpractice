# 📤 Руководство по загрузке проекта на GitHub

## ✅ Репозиторий создан на GitHub

Отлично! Теперь нужно загрузить код.

## 🚀 Способ 1: Через PowerShell скрипт (проще)

1. **Запустите скрипт:**
   ```powershell
   .\upload_to_github.ps1
   ```

2. **Следуйте инструкциям на экране**

## 🔧 Способ 2: Вручную (пошагово)

### Шаг 1: Откройте PowerShell в папке проекта

```powershell
cd C:\Users\USER\Documents\medpractice
```

### Шаг 2: Инициализируйте Git (если еще не сделано)

```powershell
git init
```

### Шаг 3: Добавьте все файлы

```powershell
git add .
```

### Шаг 4: Создайте первый коммит

```powershell
git commit -m "Initial commit"
```

### Шаг 5: Добавьте удаленный репозиторий

**Замените `YOUR_USERNAME` на ваш GitHub username!**

```powershell
git remote add origin https://github.com/YOUR_USERNAME/medpractice.git
```

### Шаг 6: Переименуйте ветку в main

```powershell
git branch -M main
```

### Шаг 7: Загрузите на GitHub

```powershell
git push -u origin main
```

## 🔐 Если запросит авторизацию

GitHub больше не принимает пароли. Нужен **Personal Access Token**:

### Как получить токен:

1. Зайдите на GitHub → Settings
2. Developer settings → Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. Название: `medpractice-upload`
5. Выберите срок действия (например, 90 дней)
6. Отметьте права: **`repo`** (полный доступ к репозиториям)
7. Generate token
8. **Скопируйте токен** (показывается только один раз!)

### При запросе пароля:

- **Username:** ваш GitHub username
- **Password:** вставьте токен (не пароль!)

## ⚠️ Важно: .gitignore

Убедитесь, что файл `.gitignore` содержит:

```
node_modules/
server/node_modules/
.env
.env.local
*.pem
server_key.pem
*.key
```

Это предотвратит загрузку секретных данных и больших папок.

## ✅ Проверка загрузки

После успешной загрузки:
1. Зайдите на GitHub
2. Откройте ваш репозиторий
3. Должны увидеть все файлы проекта

## 📋 Что дальше?

После загрузки на GitHub:
1. Клонировать на сервер
2. Настроить .env файлы
3. Установить зависимости
4. Запустить приложение

---

**Нужна помощь?** Скажите, на каком шаге возникла проблема!


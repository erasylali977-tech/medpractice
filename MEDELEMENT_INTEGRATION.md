# Интеграция с MedElement API

## 📋 Обзор

Приложение интегрировано с **MedElement API** - базой данных клинических рекомендаций и протоколов лечения МЗ РК. Это позволяет:

- ✅ Использовать актуальные протоколы лечения из базы MedElement
- ✅ Получать диагностические критерии из официальных источников
- ✅ Валидировать назначенное лечение на соответствие протоколам МЗ РК
- ✅ Автоматически обновлять протоколы при изменении рекомендаций
- ✅ Fallback на локальную базу протоколов при отсутствии API ключа

## 🔧 Настройка

### Шаг 1: Получение API ключа MedElement

1. Зарегистрируйтесь на [MedElement](https://www.medelement.com)
2. Обратитесь в поддержку для получения API доступа
3. Получите ваш API ключ (обычно это Bearer токен)

### Шаг 2: Настройка переменных окружения

Откройте файл `server/.env` и добавьте следующие переменные:

```env
# MedElement API Configuration
USE_MEDELEMENT_API=true
MEDELEMENT_API_KEY=your_api_key_here
MEDELEMENT_API_BASE=https://api.medelement.com/v1
```

**Примечание:** 
- Если `USE_MEDELEMENT_API=false` или `MEDELEMENT_API_KEY` не указан, приложение будет использовать только локальную базу протоколов
- Приложение автоматически делает fallback на локальные протоколы при недоступности API

### Шаг 3: Перезапуск сервера

После изменения `.env` файла перезапустите сервер:

```bash
cd server
npm run dev
```

## 📊 Как это работает

### Архитектура

```
┌─────────────────┐
│  Case Flow      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────────┐
│ protocols.js    │◄─────│ medelementService│
│ (локальная БД)  │      │ (API интеграция) │
└────────┬────────┘      └────────┬─────────┘
         │                        │
         └──────────┬─────────────┘
                    │
                    ▼
         ┌────────────────────┐
         │  treatmentSimulation│
         │  evaluationService │
         └────────────────────┘
```

### Процесс загрузки протокола

1. **Запрос протокола** - при проверке лечения система запрашивает протокол
2. **Проверка кэша** - сначала проверяется локальный кэш (TTL 24 часа)
3. **Запрос к MedElement API** - если в кэше нет, запрашивается из API
4. **Fallback** - если API недоступен, используется локальная база
5. **Кэширование** - загруженный протокол сохраняется в кэш

### Пример использования

```javascript
// Автоматически используется в treatmentSimulationService
const validation = await treatmentSimulationService.checkTreatmentCompliance(
  caseData, 
  treatmentData
);

// Результат содержит:
// - protocol: протокол лечения (из API или локальный)
// - medelementValidation: результаты валидации через API
// - source: 'medelement_api' или 'local'
// - errors, warnings, recommendations
```

## 🎯 Возможности

### 1. Загрузка протоколов лечения

Система автоматически загружает протоколы по МКБ-10 кодам:

- `A02.0` - Сальмонеллез
- `A03.9` - Шигеллез
- `E03.9` - Гипотиреоз
- `E05.0` - Гипертиреоз
- И другие...

### 2. Валидация лечения

При проверке назначенного лечения система:

- ✅ Сверяет препараты с протоколом МЗ РК
- ✅ Проверяет показания и противопоказания
- ✅ Выявляет ошибки назначения
- ✅ Предоставляет рекомендации

### 3. Диагностические критерии

Загружаются актуальные диагностические критерии:

- Клинические критерии
- Лабораторные исследования
- Инструментальные методы
- Дифференциальная диагностика

### 4. Информация о лекарствах

Поиск информации о препаратах в базе MedElement:

- Показания
- Противопоказания
- Дозировки
- Взаимодействия

## 🔍 Проверка работы

### Проверка конфигурации

```bash
cd server
node -e "console.log('USE_MEDELEMENT_API:', process.env.USE_MEDELEMENT_API)"
```

### Логи сервера

При запуске сервера проверьте логи:

```
✅ Протокол A02.0 загружен из MedElement API
⚠️ MedElement API недоступен, используем локальные протоколы
```

### Тестовый запрос

Создайте тестовый endpoint (опционально):

```javascript
// GET /api/test/medelement
router.get('/test/medelement', async (req, res) => {
  const protocol = await getTreatmentProtocol('A02.0');
  res.json({ 
    protocol, 
    source: protocol?.source || 'local' 
  });
});
```

## 📝 Кэширование

Протоколы кэшируются на 24 часа для оптимизации:

- Уменьшение нагрузки на API
- Быстрая загрузка протоколов
- Автоматическое обновление раз в сутки

Для очистки кэша:

```javascript
import { clearCache } from './services/medelementService.js';
clearCache();
```

## 🛠️ Разработка

### Структура файлов

```
server/src/
├── services/
│   └── medelementService.js      # Основной сервис интеграции
├── data/
│   └── protocols.js              # Локальная база протоколов
└── routes/
    └── treatmentSimulation.js    # Использует MedElement API
```

### Добавление новых модулей

Для добавления протоколов новых модулей:

1. Обновите маппинг в `protocols.js`:

```javascript
const caseToCode = {
  'new_case_id': 'I10.0', // МКБ-10 код
  // ...
};
```

2. Протоколы автоматически загрузятся из MedElement API

## ⚠️ Важные замечания

1. **API ключ** - храните в безопасности, не коммитьте в git
2. **Rate limiting** - MedElement API может иметь лимиты запросов
3. **Fallback** - всегда работает локальная база при недоступности API
4. **Кэш** - для продакшена рекомендуется использовать Redis вместо Map

## 📚 Дополнительные ресурсы

- [MedElement - Болезни](https://diseases.medelement.com)
- [MedElement - Протоколы](https://www.medelement.com/protocols)
- [Документация API MedElement](https://api.medelement.com/docs) (если доступна)

## 🐛 Решение проблем

### API недоступен

- Проверьте `MEDELEMENT_API_KEY` в `.env`
- Проверьте интернет-соединение
- Проверьте URL `MEDELEMENT_API_BASE`
- Приложение автоматически переключится на локальную базу

### Протокол не найден

- Проверьте правильность МКБ-10 кода
- Убедитесь, что протокол существует в MedElement
- Проверьте логи сервера для деталей

### Медленная загрузка

- Проверьте работу кэша
- Рассмотрите использование Redis для кэширования
- Проверьте скорость интернет-соединения

---

**Примечание:** Если у вас нет доступа к MedElement API, приложение полностью функционально с локальной базой протоколов!




# MedPractice

Медицинский симулятор для практики диагностики клинических случаев.

## Структура проекта

- `src/` - React фронтенд
- `server/` - Node.js/Express backend API

## Установка и запуск

### Backend

```bash
cd server
npm install
npm run dev
```

Backend запустится на `http://localhost:3001`

**Важно:** API ключи настраиваются через файл `.env` в папке `server/`:

Создайте файл `server/.env`:

```env
# Выберите провайдера: gemini, claude, openai
AI_PROVIDER=gemini

# Google Gemini API (работает по умолчанию)
GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic Claude API (когда получите доступ)
CLAUDE_API_KEY=your_claude_api_key_here

# OpenAI API (альтернатива)
OPENAI_API_KEY=your_openai_api_key_here

# MedElement API (опционально) - для актуальных протоколов лечения
USE_MEDELEMENT_API=false
MEDELEMENT_API_KEY=your_medelement_api_key_here
MEDELEMENT_API_BASE=https://api.medelement.com/v1
```

**По умолчанию используется Gemini** - он уже настроен и работает. Когда получите ключ Claude, просто:
1. Добавьте `CLAUDE_API_KEY=ваш_ключ` в `.env`
2. Измените `AI_PROVIDER=claude`
3. Перезапустите сервер

**Проверка работы backend:**
- Откройте в браузере: `http://localhost:3001/api/health`
- Должен вернуться JSON: `{"status":"ok","message":"MedPractice API is running"}`
- Или проверьте: `http://localhost:3001/api/cases` - должен вернуть массив кейсов

### Frontend

```bash
npm install
npm start
```

Frontend запустится на `http://localhost:3000`

### Запуск всего вместе

```bash
npm install
cd server && npm install && cd ..
npm run dev
```

Это запустит и backend, и frontend одновременно.

## API Endpoints

- `GET /api/health` - проверка работоспособности
- `GET /api/cases` - получить все клинические кейсы
- `GET /api/cases/:id` - получить конкретный кейс
- `POST /api/chat` - получить ответ пациента на вопрос врача (использует Google Gemini AI)
- `POST /api/analysis` - получить AI анализ работы врача после завершения кейса

## Технологии

- Frontend: React 19, Lucide React
- Backend: Node.js, Express
- Хранение: localStorage (frontend), файлы (backend)

## Система авторизации

Приложение теперь использует систему регистрации и авторизации:

1. **При первом запуске** вы увидите экран входа
2. **Зарегистрируйтесь** или войдите, если уже есть аккаунт
3. **Если видите старый интерфейс** - очистите localStorage:
   - Откройте консоль (F12)
   - Выполните: `localStorage.clear()`
   - Обновите страницу (Ctrl+F5)

## Подписки

Доступны 3 типа подписок:
- **Free** - 5 токенов, базовые кейсы
- **Basic** - 50 токенов/мес, все базовые кейсы (499₽/мес)
- **Premium** - 150 токенов/мес, все кейсы (999₽/мес)

Интеграция с платежным шлюзом будет добавлена позже.

## Интеграция с MedElement API

Приложение поддерживает интеграцию с **MedElement API** для получения актуальных протоколов лечения и клинических рекомендаций МЗ РК.

**Что это дает:**
- ✅ Актуальные протоколы лечения из базы MedElement
- ✅ Автоматическая валидация назначенного лечения
- ✅ Диагностические критерии из официальных источников
- ✅ Fallback на локальную базу при отсутствии API ключа

**Настройка:**
1. Получите API ключ от MedElement
2. Добавьте в `server/.env`:
   ```env
   USE_MEDELEMENT_API=true
   MEDELEMENT_API_KEY=your_key_here
   ```
3. Перезапустите сервер

📖 **Подробная документация:** см. [MEDELEMENT_INTEGRATION.md](./MEDELEMENT_INTEGRATION.md)

**Важно:** Приложение полностью функционально без MedElement API - используется локальная база протоколов.


# ✅ Чеклист для деплоя MedPractice

## 🔍 Проверка перед деплоем

### 1. Конфигурация окружения

#### Backend (.env файл в `server/`)
```env
# AI Provider (gemini, claude, openai)
AI_PROVIDER=gemini

# API Keys
GEMINI_API_KEY=your_key_here
CLAUDE_API_KEY=your_key_here  # опционально
OPENAI_API_KEY=your_key_here  # опционально

# MedElement API (опционально)
USE_MEDELEMENT_API=false
MEDELEMENT_API_KEY=your_key_here
MEDELEMENT_API_BASE=https://api.medelement.com/v1

# Server
PORT=3001
NODE_ENV=production

# CORS (для продакшена)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

#### Frontend (.env файл в корне проекта)
```env
REACT_APP_API_URL=https://api.yourdomain.com
# или если API на том же домене:
REACT_APP_API_URL=/api
```

### 2. Безопасность

#### ⚠️ КРИТИЧНО: Настроить CORS для продакшена

**Текущая проблема:** В `server/src/index.js` используется `app.use(cors())` без ограничений - это небезопасно для продакшена!

**Исправление:**
```javascript
// server/src/index.js
import cors from 'cors';

const corsOptions = {
  origin: process.env.NODE_ENV === 'production' 
    ? process.env.ALLOWED_ORIGINS?.split(',') || ['https://yourdomain.com']
    : true, // В разработке разрешаем все
  credentials: true,
  optionsSuccessStatus: 200
};

app.use(cors(corsOptions));
```

#### ⚠️ КРИТИЧНО: Улучшить JWT токены

**Текущая проблема:** Используется упрощенная генерация токенов в `server/src/services/authService.js`

**Рекомендация:** Установить библиотеку `jsonwebtoken`:
```bash
cd server
npm install jsonwebtoken
```

И обновить `generateToken` и `verifyToken` функции.

#### ⚠️ ВАЖНО: Защита от SQL Injection / XSS

- ✅ Пароли хешируются (pbkdf2)
- ✅ JSON парсинг с обработкой ошибок
- ⚠️ Нет rate limiting - добавить для API endpoints
- ⚠️ Нет валидации входных данных в некоторых местах

### 3. Хранение данных

**Текущее состояние:** Используются JSON файлы (`server/data/users.json`, `server/data/analytics.json`)

**Для продакшена рекомендуется:**
- MongoDB или PostgreSQL для пользователей и платежей
- Redis для сессий и кэширования
- Файловое хранилище для аналитики (можно оставить JSON)

**Минимальные требования для закрытого тестирования:**
- ✅ JSON файлы работают для небольшого количества пользователей (< 1000)
- ⚠️ Нужно настроить резервное копирование
- ⚠️ Нужно настроить логирование

### 4. Обработка ошибок

#### ✅ Хорошо:
- Большинство API routes имеют try-catch
- Frontend обрабатывает ошибки сети
- Есть fallback значения

#### ⚠️ Нужно улучшить:
- Добавить централизованное логирование ошибок
- Добавить мониторинг (Sentry, LogRocket)
- Улучшить сообщения об ошибках для пользователей

### 5. Производительность

#### ⚠️ Проблемы:
- Нет кэширования для статических данных (кейсы, модули)
- Нет rate limiting
- Нет оптимизации изображений
- Много console.log в продакшене (нужно убрать или использовать logger)

### 6. Функциональность

#### ✅ Работает:
- Регистрация и авторизация
- Прохождение кейсов (CaseFlow)
- AI Mentor
- Система платежей (полуавтоматическая)
- Аналитика
- Мультиязычность (ru, kk, en)

#### ⚠️ Проверить:
- [ ] Все кейсы загружаются корректно
- [ ] AI ответы работают стабильно
- [ ] Платежи обрабатываются правильно
- [ ] Статистика обновляется корректно
- [ ] Медкоины списываются и начисляются правильно

### 7. Деплой на сервер

#### Структура деплоя:

```
/var/www/medpractice/
├── frontend/          # React build
├── backend/           # Node.js server
└── nginx/             # Nginx конфигурация
```

#### Nginx конфигурация:

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # Frontend
    location / {
        root /var/www/medpractice/frontend;
        try_files $uri $uri/ /index.html;
    }
    
    # Backend API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

#### PM2 для Node.js процесса:

```bash
# Установка PM2
npm install -g pm2

# Запуск
cd /var/www/medpractice/backend
pm2 start src/index.js --name medpractice-api

# Сохранение конфигурации
pm2 save
pm2 startup
```

### 8. Резервное копирование

**Критичные файлы для бэкапа:**
- `server/data/users.json` - пользователи
- `server/data/analytics.json` - аналитика
- `server/.env` - конфигурация (хранить безопасно!)

**Рекомендация:** Настроить автоматический бэкап:
```bash
# Скрипт бэкапа
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
tar -czf /backup/medpractice_$DATE.tar.gz /var/www/medpractice/backend/data
```

### 9. Мониторинг

**Рекомендуется настроить:**
- Uptime monitoring (UptimeRobot, Pingdom)
- Error tracking (Sentry)
- Server monitoring (PM2 monitoring, htop)
- Log aggregation (ELK, или просто логи в файлы)

### 10. Тестирование перед запуском

#### Чеклист тестирования:

- [ ] Регистрация нового пользователя
- [ ] Вход существующего пользователя
- [ ] Загрузка модулей и кейсов
- [ ] Прохождение полного кейса (3 фазы)
- [ ] AI ответы работают
- [ ] Создание платежа
- [ ] Активация подписки (админ)
- [ ] Начисление/списание медкоинов
- [ ] Мультиязычность (переключение языков)
- [ ] Аналитика (просмотр статистики)
- [ ] AI Mentor работает
- [ ] Мобильная версия (responsive)

### 11. Известные проблемы и TODO

#### Критичные:
1. **CORS не настроен для продакшена** - нужно исправить
2. **JWT токены упрощенные** - рекомендуется использовать библиотеку
3. **Нет rate limiting** - может быть DDoS атака
4. **Много console.log** - нужно убрать или заменить на logger

#### Важные:
1. **JSON файлы для данных** - для продакшена лучше БД
2. **Нет автоматического бэкапа** - нужно настроить
3. **Нет мониторинга ошибок** - рекомендуется добавить
4. **Нет оптимизации изображений** - можно улучшить

#### Желательные:
1. **Кэширование** - для статических данных
2. **CDN** - для статических файлов
3. **SSL сертификат** - обязательно для HTTPS
4. **Compression** - gzip для API ответов

### 12. Команды для деплоя

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/medpractice.git
cd medpractice

# 2. Установить зависимости
npm install
cd server && npm install && cd ..

# 3. Настроить .env файлы
# server/.env и .env (для frontend)

# 4. Собрать frontend
npm run build

# 5. Запустить backend
cd server
pm2 start src/index.js --name medpractice-api

# 6. Настроить Nginx (см. выше)

# 7. Проверить работу
curl https://yourdomain.com/api/health
```

### 13. После деплоя

1. ✅ Проверить все функции
2. ✅ Настроить мониторинг
3. ✅ Настроить бэкапы
4. ✅ Проверить логи
5. ✅ Протестировать на разных устройствах
6. ✅ Проверить скорость загрузки
7. ✅ Проверить безопасность (SSL, CORS)

---

## 📝 Быстрый чеклист перед запуском

- [ ] Все .env файлы настроены
- [ ] CORS настроен для продакшена
- [ ] SSL сертификат установлен
- [ ] Nginx настроен
- [ ] PM2 запущен
- [ ] Бэкап настроен
- [ ] Мониторинг настроен
- [ ] Все функции протестированы
- [ ] Логи проверены
- [ ] Домен настроен

---

**Дата создания:** $(date)
**Версия:** 1.0.0


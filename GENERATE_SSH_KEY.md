# 🔑 Генерация нового SSH ключа

## Вариант 1: Через PowerShell (Windows)

```powershell
# Сгенерировать новый SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com" -f C:\Users\USER\Documents\medpractice\server_key

# Это создаст два файла:
# - server_key (приватный ключ) - используйте этот для подключения
# - server_key.pub (публичный ключ) - добавьте на сервер
```

## Вариант 2: Через Git Bash (если установлен Git)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com" -f ~/server_key
```

## После генерации

1. **Приватный ключ** (`server_key`) - используйте для подключения:
   ```cmd
   ssh -i C:\Users\USER\Documents\medpractice\server_key root@85.198.88.102
   ```

2. **Публичный ключ** (`server_key.pub`) - нужно добавить на сервер:
   - Подключитесь по паролю (если есть)
   - Добавьте публичный ключ в `~/.ssh/authorized_keys` на сервере

## Добавление публичного ключа на сервер

После подключения по паролю выполните:

```bash
# Создать папку .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавить публичный ключ
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Или скопируйте содержимое `server_key.pub` и добавьте в файл на сервере.


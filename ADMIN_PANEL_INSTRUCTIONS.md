# 🔧 ИНСТРУКЦИЯ ПО АДМИН-ПАНЕЛИ ОПЛАТЫ

## 📋 Как использовать:

### 1. Доступ к админ-панели

Админ-панель доступна по ссылке:
```
http://localhost:3000/admin/payments
```

Или добавьте в меню профиля кнопку "Админ-панель".

### 2. Процесс активации подписки

1. Пользователь оплачивает через Kaspi Pay с комментарием (User ID)
2. Заходите в админ-панель `/admin/payments`
3. Видите список ожидающих платежей:
   - Email пользователя
   - User ID из комментария
   - Тариф (Plus/Pro)
   - Сумма
   - Дата создания
4. Нажимаете **"Активировать подписку"**
5. Подписка активируется автоматически!
6. Пользователю начисляются медкоины

### 3. Что происходит при активации

- ✅ Подписка активируется (Plus или Pro)
- ✅ Начисляются медкоины (3000 для Plus, 5000 для Pro)
- ✅ Статус платежа меняется на "activated"
- ✅ Записывается дата активации

---

## 🚀 Быстрый старт:

### Шаг 1: Отключите тестовый режим

В браузере (консоль):
```javascript
localStorage.setItem('TEST_MODE', 'false');
location.reload();
```

Или в `src/components/PricingPage.js`:
```javascript
const [isTestMode, setIsTestMode] = useState(false); // Изменить на false
```

### Шаг 2: Добавьте ссылку на админ-панель

В `src/App.js` добавьте роут:
```javascript
{currentScreen === 'admin-payments' && (
  <AdminPayments userData={userData} />
)}
```

### Шаг 3: Тестируйте

1. Пользователь оформляет подписку → открывается модальное окно
2. Пользователь оплачивает через Kaspi Pay с User ID в комментарии
3. Вы видите платеж в админ-панели
4. Нажимаете "Активировать" → готово!

---

## 📝 Структура платежа:

```javascript
{
  id: "payment_1234567890_abc123",
  userId: "user_12345",
  email: "user@example.com",
  planId: "plus", // или "pro"
  planName: "Plus",
  amount: 3800,
  medcoins: 3000,
  currency: "KZT",
  status: "pending", // pending, activated, cancelled
  userComment: "user_12345", // User ID из комментария
  kaspiPayLink: "https://pay.kaspi.kz/pay/oncz5m8v",
  createdAt: "2025-01-23T10:00:00Z",
  activatedAt: null,
  activatedBy: null
}
```

---

## ⚠️ Важно:

1. **User ID в комментарии обязателен** - без него невозможно найти пользователя
2. **Платежи хранятся в памяти** - при перезапуске сервера они пропадут
3. **В продакшене** нужно использовать БД (MongoDB/PostgreSQL)
4. **Проверяйте админ-панель регулярно** - платежи не активируются автоматически

---

## 🔄 Автообновление:

Админ-панель автоматически обновляется каждые 30 секунд.

Также можно обновить вручную кнопкой "Обновить".

---

## 📧 Уведомления (будущее):

Планируется добавить:
- Email уведомления при новом платеже
- Telegram бот для уведомлений
- Webhook от Kaspi Pay (когда будет доступен API)

---

**Ссылка Kaspi Pay:** https://pay.kaspi.kz/pay/oncz5m8v




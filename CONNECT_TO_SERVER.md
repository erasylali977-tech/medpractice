# 🔐 Подключение к серверу

## ✅ Найден приватный ключ!

Ваш приватный ключ находится в: `C:\Users\USER\.ssh\id_ed25519`

## Команда для подключения

```cmd
ssh -i C:\Users\USER\.ssh\id_ed25519 root@85.198.88.102
```

Или из PowerShell:

```powershell
ssh -i C:\Users\USER\.ssh\id_ed25519 root@85.198.88.102
```

## ⚠️ Важно: Добавить публичный ключ на сервер

Если подключение не работает (Permission denied), значит публичный ключ еще не добавлен на сервер.

### Вариант 1: Через панель управления vStack Cloud

1. Зайдите в панель управления сервером
2. Найдите раздел "SSH ключи" или "Ключи доступа"
3. Добавьте публичный ключ:
   ```
   ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHNehiebuNVEE1UMQ8hKoTSvE2rkEu3ibkb1qZhBQHKc
   ```

### Вариант 2: Если есть доступ через веб-консоль

1. Зайдите в веб-консоль сервера через панель управления
2. Выполните команды:
   ```bash
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHNehiebuNVEE1UMQ8hKoTSvE2rkEu3ibkb1qZhBQHKc" >> ~/.ssh/authorized_keys
   chmod 600 ~/.ssh/authorized_keys
   ```

## После успешного подключения

Когда подключитесь к серверу, выполните:

```bash
# 1. Обновить систему
apt update && apt upgrade -y

# 2. Установить Node.js 18.x
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt install -y nodejs

# 3. Проверить установку
node --version
npm --version

# 4. Установить Nginx
apt install -y nginx

# 5. Установить PM2 для управления Node.js процессами
npm install -g pm2

# 6. Установить Git (если нужно)
apt install -y git
```

## Проверка подключения

После выполнения команды SSH вы должны увидеть:
```
root@server:~#
```

Это означает успешное подключение!

